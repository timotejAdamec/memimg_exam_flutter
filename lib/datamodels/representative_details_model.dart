class RepresentativeDetailsModel {
  final List infoArr;
  final String imageUrl;

  RepresentativeDetailsModel({
    this.infoArr,
    this.imageUrl,
  });

/*  EpisodeItemModel.fromJson(Map<String, dynamic> map)
      : title = map['title'],
        duration = map['duration'],
        imageUrl = map['imageUrl'];*/
}
